name: coldchain_guardian
description: Industrial freezer and chiller tracking logbook.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2
  google_generative_ai: ^0.4.0
  uuid: ^4.3.3
  intl: ^0.19.0
  http: ^1.2.0
  provider: ^6.1.1
  lucide_icons: ^0.3.1
  fl_chart: ^0.66.0    # For temperature graphs
  image_picker: ^1.0.7 # For camera

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
